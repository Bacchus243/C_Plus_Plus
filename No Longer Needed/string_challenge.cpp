/*
String Challenge

Have the function StringChallenge(str1,str2) take both parameters being passed 
and return the string true if a portion of str1 characters can be rearranged 
to match str2, otherwise return the string false. For example: if str1 
is "rkqodlw" and str2 is "world" the output should return true. Punctuation 
and symbols will not be entered with the parameters.


Examples:

Input: "cdore" & str2 = "coder" 
Output: true


Input: "h3llko" & str2 = "hello" 
Output: false

*/


#include <stdio.h> 
#include <string.h>

void StringChallenge(char * str1[], char str2[]) {
  
  // code goes here  
  printf("%s", str1);

}

int main(void) { 
   
  // keep this function call here
  StringChallenge(coderbyteInternalStdinFunction(stdin));
  return 0;
    
}